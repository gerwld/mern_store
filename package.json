{
  "name": "mern_store",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development nodemon ./backend/server.js",
    "build": "pnpm install && pnpm install --dir frontend && npm run build --prefix frontend",
    "build:npm": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node ./backend/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
